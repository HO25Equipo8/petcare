package com.petcare.back.repository;
import com.petcare.back.domain.entity.IncidentImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IncidentsImageRepository extends JpaRepository<IncidentImage,Long> {
}
