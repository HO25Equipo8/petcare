package com.petcare.back.domain.enumerated;

public enum PlanType {
    BASE,       // acceso limitado
    PREMIUM,    // acceso a actualizaciones en vivo
    PRO         // acceso a actualizaciones + tracking en tiempo real
}
